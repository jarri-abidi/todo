version: "2"
sql:
  - engine: "postgresql"
    queries: "postgres/queries/"
    schema: "postgres/migrations/"
    gen:
      go:
        package: "gen"
        out: "postgres/gen"
        emit_empty_slices: true
